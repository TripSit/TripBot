// import {
//   Client,
// } from 'discord.js';
// import env from './env.config';
// import {embedTemplate} from '../../discord/utils/embedTemplate';
// import {stripIndents} from 'common-tags';
import { parse } from 'path';
import log from './log'; // eslint-disable-line @typescript-eslint/no-unused-vars

const PREFIX = parse(__filename).name; // eslint-disable-line @typescript-eslint/no-unused-vars

export default template;

/**
 * Template
 * @param {Client} client Template
 */
export async function template(/* client:Client */) {
// log.debug(`[${PREFIX}] Initialized!`);
// log.debug(`[${PREFIX}] client: ${JSON.stringify(client, null, 2)}`);
}
