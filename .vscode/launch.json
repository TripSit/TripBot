{
    "version": "<some-version>",
    "configurations": [
        {
            "type": "node",
            "runtimeVersion": "18.0.0",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/src/index.js"
        }
    ]
}