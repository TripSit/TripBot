version: '3.7'
services:
  tripbot:
    build:
      dockerfile: Dockerfile
    container_name: tripsit_discord_bot
    restart: unless-stopped
    ports:
      - "8080:8080"
networks:
  default:
    name: http-api_default
    external: true