kind: pipeline
type: docker
name: Deploy TripBot

steps:
  - name: Rebuild TripBot
    image: docker
    commands:
      - apk update
      - apk upgrade
      - apk add --no-cache git
      - docker compose -f docker-compose.host.yml --project-name tripsit-live up tripbot -d --force-recreate --build
      # - tail -f /dev/null
    environment:
      DOCKER_HOST:
        from_secret: DOCKER_HOST
      DOCKER_BUILDKIT:
        from_secret: DOCKER_BUILDKIT

  - name: discord notification
    image: appleboy/drone-discord
    settings:
      webhook_id: 1123644407434973255
      webhook_token: 73CXRJKNIwOUHyiVWQiJ4lxUllY2__BMH_lIrB6_XdTDH7dAPk0b_MZ3-TQAXiAj7cH6
      avatar_url: https://i.imgur.com/tnw3I34.png
      username: 'Drone CI'
      message: |
        {{#success build.status}}
        Build {{build.number}} succeeded.
        {{else}}
        Build {{build.number}} failed.
        {{/success}}
        Build time: {{build.started}}
        Commit message: {{commit.message}}
        Commit author: {{commit.author}}
        Commit branch: {{commit.branch}}
        {{#success build.status}}
        View logs: <{{build.link}}>
        {{else}}
        View failed job: <{{build.link}}>
        {{/success}}

trigger:
  branch:
  - Docker-update
trigger:
  event:
  # - cron
  - custom
  - push
  # - pull_request
  # - tag
  # - promote
  # - rollback